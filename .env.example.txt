# Supabase project settings for the browser client.
# Copy this file to .env for local tooling or configure these values in your hosting provider.
# The anon key is safe to expose in browser code when Row Level Security policies are enabled.
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your-supabase-anon-key

# Optional: used by future deployment/build tooling to generate canonical SEO URLs.
SITE_URL=https://www.memarsfamily.org
